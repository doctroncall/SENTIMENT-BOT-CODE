# Trading Sentiment Analysis System - Python Dependencies
# Install with: pip install -r requirements.txt

# Core Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Excel Support
openpyxl>=3.1.0

# MetaTrader5 Integration (Windows only)
MetaTrader5>=5.0.0; sys_platform == 'win32'

# Process monitoring for MT5 connection diagnostics (optional but recommended)
psutil>=5.9.0

# MT5-only; Yahoo Finance removed

# PDF Report Generation (optional)
reportlab>=4.0.0

# GUI Support (usually included with Python on Windows)
# tkinter - included with standard Python installation

# Streamlit for web-based GUI (optional)
streamlit>=1.28.0
